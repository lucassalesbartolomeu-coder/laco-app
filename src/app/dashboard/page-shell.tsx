// ─── Dashboard Shell Page ─────────────────────────────────────────────────────
// Adicione este componente ao seu dashboard/page.tsx existente para renderizar
// o phone shell. Não substitui a lógica atual — é um wrapper visual.
import DashboardClient from "@/components/app-shell/DashboardClient";
export default function DashboardShellPage() {
  return <DashboardClient />;
}
